import { DataSourceWrapper } from './data-source-wrapper';

describe('DataSourceWrapper', () => {
  it('should create an instance', () => {
    expect(new DataSourceWrapper()).toBeTruthy();
  });
});
